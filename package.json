{
  "name": "demo-board",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Outcome-first demo board: annotated screenshots on a zoomable canvas.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:all": "npm run test && npm run test:e2e",
    "sample": "node scripts/make-sample.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "fflate": "^0.8.3"
  }
}
